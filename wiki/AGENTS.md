# Heifer Zephyr Instrument Wiki Agent Guide

This wiki is the living knowledge layer for the Heifer Zephyr musical instrument design library. The library and explorer pages show *what exists*; this wiki explains *what has been learned*, *why decisions were made*, *how projects relate to each other*, and *what still needs investigation*.

Schema version: **v2 (2026-05-18)**. v1 pages (kora, cajon, wood-shell-tongue-drum) are valid and will be migrated opportunistically on next ingest.

## Operating Model

- Raw sources are source of truth. Read them, cite them, and do not rewrite them from inside this wiki workflow.
- Wiki pages are LLM-maintained synthesis. Update pages when new sources, repo changes, or user questions reveal better structure.
- The wiki compounds. When a query produces a useful comparison, roadmap, contradiction note, or design pattern, file it back into the wiki instead of leaving it only in chat (see Answer-as-Page Workflow below).
- Do not invent measurements, release status, CAD readiness, or validation evidence. If a claim is not present in a repo, source note, or user instruction, mark it as an open question.
- Cite, do not copy. Prefer Obsidian-style wikilinks for wiki↔wiki references, and ordinary markdown links into repos, raw sources, and generated artifacts.

## Retrieval Layer (QMD)

This wiki and its source streams are indexed by [qmd](https://github.com/tobi/qmd) — a local hybrid BM25 + vector + rerank search engine over markdown. Use it before grep when looking for anything that may already live in the wiki or its source streams.

Collections available:

- `instrument-wiki` — this wiki (curated synthesis pages)
- `instrument-builds` — `instrument-maker/` build packets, prototype logs, validation
- `sheet-music` — `sheet-music/` registry, songsheets, fingering charts
- `makerspace` — `makerspace/` jigs, fixtures, shop docs
- `reverse-engineering` — `reverse-engineering/` RE notes
- `laser-cut` — `laser-cut/` LightBurn / laser references
- `learn-to-play` — per-instrument songbooks, fingering charts, beginner tunes (grouped by family: woodwinds / strings / drums / idiophones)
- `habitat-maker` — wildlife habitat design skill + per-species build repos (birdhouses, bat-houses, bee-houses, pollinator-habitat, garden infrastructure)
- `woodworking` — `woodworking/` knowledge — joinery, jigs, finishing
- `obsidian` — allowlist-scoped slice of the Obsidian Second_Brain vault (instruments / making / outdoors / wrfcoin / clippings)
- `sprint-docs` — `docs/` cross-cutting sprint plans, dated rounds, partner-peek and handoff documents

The WRFCoin side adds four more collections (`wrfcoin-wiki`, `core4-docs`, `audits`, `sprint-handoffs`) on the same qmd index — visible via `qmd collection list`.

Directory contexts mirror the wiki's directory contract, so passage results carry the right topic context up the tree.

Common commands:

```bash
qmd search "<keywords>" -c instrument-wiki -n 10   # fast BM25 (use when you know terms)
qmd query "<natural language question>"             # hybrid + rerank across all collections (best quality)
qmd get "<path>"                                    # fetch a full document
qmd status                                          # collection + embedding health
```

After ingesting new wiki pages, run `qmd update` (incremental re-index) and `qmd embed` (incremental) so new content becomes searchable. `qmd embed` is CPU-bound on first run only.

## Directory Contract

```text
wiki/
  AGENTS.md
  index.md
  log.md
  instruments/        One page per instrument repo, named <slug>.md.
  acoustic-classes/   Cross-instrument acoustic model pages.
  fabrication/        Process, tooling, fixture, and material workflow pages.
  materials/          Material behavior and sourcing pages.
  synthesis/          Portfolio-level analysis, decision records, answer-as-page outputs.
  sources/            Source summaries when a raw source deserves a stable distillation.
raw-sources/
  inbox/              Drop new sources here for ingest queue.
  processed/<YYYY-MM>/  Inbox items move here after ingest.
  second-brain/       Symlink → C:\Users\Tony\Documents\Second_Brain (allowlisted).
  obsidian-clips/     Symlink → C:\Users\Tony\Documents\Second_Brain\Clippings.
  .ingest-allowlist   Newline-separated path globs the LLM may read.
data/
  wiki-manifest.json  Generated. Library bridge.
  library-manifest.json  Generated. Library control surface.
```

Machine-readable wiki state lives at `data/wiki-manifest.json`. It is generated by `scripts/generate_library.py` and consumed by `site/library.html`. A complementary `scripts/generate_wiki.py` produces a browsable `site/wiki.html`.

## Frontmatter Schema (v2)

Every wiki page MUST start with YAML frontmatter. Fields:

```yaml
---
# Identity
title: Kora-Inspired 21-String Prototype
slug: kora                         # for instrument pages; folder/filename otherwise
wiki_type: instrument              # see Type Values below
status: active                     # see Status Values below

# Provenance
sources:
  - path: ../../../kora/README.md
    kind: repo                     # repo | obsidian | clip | web | photo | cad | spreadsheet | wolfram
    last_seen: 2026-05-18
  - path: second-brain/Instruments/Kora field notes 2025-11.md
    kind: obsidian
    last_seen: 2026-05-18

# Wiki graph
crosslinks:
  - acoustic-classes/harp-lute
  - synthesis/public-release-blockers
  - fabrication/stave-construction

# Knowledge state
open_questions:
  - "Cultural-provenance wording review (consult sources, finalize copy)."
  - "Bridge/head staged load test plan."
answers_filed:                     # answer-as-page outputs originating from this page
  - synthesis/string-tension-tradeoffs

# Maintenance
last_ingest: 2026-05-18            # date a source was last integrated
last_lint: 2026-05-18              # date the page was last health-checked
review_due: 2026-08-18             # optional; if set, lint surfaces stale pages

# Search/UI
tags: [instrument, string, harp-lute, private-review]
---
```

### Type values (`wiki_type`)

| Value | Folder | Purpose |
| --- | --- | --- |
| `index` | `wiki/` root | Index pages and navigation hubs |
| `instrument` | `instruments/` | One per repo |
| `acoustic-class` | `acoustic-classes/` | Cross-instrument acoustic model |
| `fabrication` | `fabrication/` | Process / tooling / fixture / workflow |
| `material` | `materials/` | Material behavior, sourcing, properties |
| `synthesis` | `synthesis/` | Portfolio analysis, decision record, comparison |
| `source-distillation` | `sources/` | Stable LLM-readable summary of a raw source |

### Status values

- `stub` — page exists but has not had a real source ingest yet.
- `active` — contains integrated notes from one or more real sources.
- `review-needed` — claims need human review, source reconciliation, or freshness check.
- `stale` — `review_due` has passed; surface in next lint pass.
- `missing` — reserved for generated manifests; never set in frontmatter.

### Counts derived by the generator

`generate_library.py` derives `source_count = len(sources)` and `open_questions = len(open_questions)`. Pages MAY also persist these as scalar fields for back-compat with v1 pages and Dataview; if both exist, the array length wins.

## Page Conventions

- Use Obsidian-style links for wiki relationships: `[[acoustic-classes/harp-lute]]`.
- Use ordinary markdown links for files, repo paths, web sources, and generated artifacts.
- Stable section headings (so future agents can find their place): `Overview`, `Current Status`, `Source Notes`, `Design Knowledge`, `Cross-Links`, `Open Questions`, `Maintenance Notes`.
- Concise bullets over long prose when capturing source facts.
- Put uncertainties in `Open Questions`, not hidden in vague language.
- Each `Source Notes` bullet SHOULD start with the source kind in brackets, e.g. `[repo] kora/README.md — bridge geometry settled at 11 mm spacing after third prototype.`

## Ingest Workflow

1. Identify the raw source and record where it came from (repo path, Obsidian wikilink, web URL).
2. Verify the source is allowed by `raw-sources/.ingest-allowlist`.
3. Read the relevant existing wiki pages before writing.
4. Create or update a `wiki/sources/<slug>.md` summary if the source is substantial (multi-page PDF, dense spreadsheet, long article).
5. Update the instrument, acoustic-class, fabrication, material, or synthesis pages touched by the source.
6. Update `wiki/index.md` if new pages were added.
7. Update `last_ingest` in the frontmatter of every touched page.
8. Append an entry to `wiki/log.md` using the chronological entry format.
9. If the source came from `raw-sources/inbox/`, move it to `raw-sources/processed/<YYYY-MM>/`.
10. Run `python3 scripts/generate_library.py` and `python3 scripts/generate_wiki.py` from `instrument-showcase/`.
11. Run `qmd update` and `qmd embed` so the new pages become searchable. Both are incremental — only changed chunks are re-processed.

## Query Workflow

1. Read `wiki/index.md` first.
2. Search via qmd: `qmd query "<question>"` for hybrid + rerank, or `qmd search "<term>" -c instrument-wiki` for BM25. Add `-c instrument-builds` (or another collection) to scope to a source stream. Fall back to `rg` only when qmd returns nothing.
3. Answer with citations to wiki pages and source files.
4. **Decide if the answer is reusable.** If yes → Answer-as-Page Workflow.

## Answer-as-Page Workflow

When a query produces useful reusable knowledge — a comparison across instruments, a roadmap, a contradiction note, a design pattern — file it back into the wiki instead of letting it disappear into chat history.

1. Pick a folder: usually `synthesis/`, occasionally `acoustic-classes/` or `fabrication/`.
2. Create `wiki/<folder>/<slug>.md` with full frontmatter. Set `wiki_type` appropriately and `status: active`.
3. In `sources:`, list the wiki pages and raw sources that supported the answer.
4. Add `answers_filed: [<this-page>]` to the originating page(s) so the audit trail is bidirectional.
5. Append a log entry of type `answer-as-page` (see log format below).
6. Run the generators.

Heuristic for promoting: if you'd be willing to write the same answer again in two weeks, file it now.

## Lint Workflow

Run periodically (monthly is a good cadence; can be scheduled). Walks the wiki and surfaces:

- **Missing pages.** Active repos with no `wiki/instruments/<slug>.md`.
- **Stale pages.** `review_due` in the past, or `last_ingest` older than 90 days for `active` pages.
- **Orphans.** Pages with zero inbound and zero outbound `crosslinks`.
- **Hot crosslink targets without a page.** Wikilinks that point to a non-existent page.
- **`review-needed` backlog.** All pages currently in that status, oldest first.
- **Acoustic classes with multiple instruments but no synthesis page.** e.g. 3+ instruments in `tongue-drum-ceramic` and no class page yet.
- **Open-question candidates for closure.** Questions that can be resolved by a fresh source ingest, measurement, CAD export, or web lookup.
- **Contradictions.** Same claim with different values across two pages (e.g. bridge spacing).

After lint, update `last_lint` on every touched page and append a `lint` log entry summarizing findings.

Run lint by asking the agent: *"Run wiki lint. Read index.md, walk the manifest, surface findings, propose next ingests."*

## Obsidian / Second_Brain Bridge

The Obsidian vault at `C:\Users\Tony\Documents\Second_Brain` and its clippings subfolder `C:\Users\Tony\Documents\Second_Brain\Clippings` are *source streams*, not the wiki itself.

**Three patterns operating in parallel:**

1. **Symlink + allowlist.** `raw-sources/second-brain/` is a symlink to the vault. `raw-sources/.ingest-allowlist` controls which subpaths the LLM may read. Default policy: allowlist `Instruments/`, `Clippings/`, `Daily Notes/2025-*`, `Daily Notes/2026-*`. Everything else is off-limits unless explicitly added.
2. **Inbox.** Web Clipper drops new pages into `Clippings/`, which is also reachable via the symlink. On each session start, the agent SHOULD list new files in `raw-sources/obsidian-clips/` against the existing wiki and decide which to integrate.
3. **Wikilink citation.** When citing an Obsidian note inside a wiki page, prefer the relative path that resolves both for the LLM and for Obsidian (`second-brain/Instruments/Kora field notes 2025-11.md`). When the user opens the wiki page inside Obsidian, the link should resolve to their actual vault note.

The agent MUST NOT modify, delete, or rename files inside `raw-sources/second-brain/`. The vault is read-only from the wiki's perspective.

## Log Entry Format

Single-line prefix so `grep "^## \[" log.md | tail -10` gives a clean recent-activity view.

```markdown
## [2026-05-18] ingest | Source Title
## [2026-05-18] query | Question summary
## [2026-05-18] answer-as-page | Page Title
## [2026-05-18] lint | Findings summary
## [2026-05-18] scaffold | Setup summary
## [2026-05-18] migrate | Schema upgrade summary
```

Body fields: `Source`, `Pages touched`, `Summary`, `Follow-ups`.

## Relationship To The Library

`scripts/generate_library.py` scans `wiki/instruments/<slug>.md` for each instrument repo. The library card claims a wiki page exists only when that exact markdown page exists. Frontmatter enriches the card with status, source count, open-question count, and last-updated date.

`scripts/generate_wiki.py` produces `site/wiki.html` — a browsable HTML rendering of the wiki in the Heifer Zephyr brand. This is the *public* face for non-Obsidian readers; Obsidian remains the *editing* face. Same files, two views.

## Migration Notes (v1 → v2)

v1 pages have scalar `source_count` and `open_questions` ints, no `sources[]` array, and no `crosslinks[]`, `last_ingest`, `last_lint`, or `review_due` fields. They are valid. On next ingest into a v1 page, the agent SHOULD:

1. Convert `source_count` / `open_questions` to arrays.
2. Add `crosslinks` from the page's actual `[[wikilinks]]`.
3. Set `last_ingest` to today.
4. Leave `last_lint` and `review_due` unset until lint runs.

The generator handles both shapes.
