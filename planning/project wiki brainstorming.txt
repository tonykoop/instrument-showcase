Help me figure out a way to incorporate Karpathy's gist on a LLM WIKI to live alongside my expansive musical instrument design library. I just had another agent start making explorer.html pages for each musical instrument to highlight all of the deliverables and to make an easier way to digest the awesome work that went into engineering each musical instrument. With 50+ musical instruments in parallel development, I needed a library.html to navigate those 50+ explorer.htmls to have better clarity, oversight, and navigation through my design library, kind of like a project database management, like soldiworks PDM but customized for my heifer zephyr artist brand. 

AND! This seems like the perfect entry point to tie the library and llm wiki together, can you help me brainstorm how to start my llm wiki? And integrate it with my library.html? Also know that I have a lot of project knowledge in my second_brain and obsidian which may be relevant for tying into these LLM wikis as we get them set up.  I might also want to consider a separate but linked LLM wiki for my wrfcoin project. 

---

Library shipped — 32 of 33 explorers now cross-link to the central library.

View library.html — open this for the central landing. 48 instrument cards organized by family (23 wind · 11 idiophone · 8 drum · 6 string), with text search and four filter axes:

Family + acoustic class

Release status (Public-release blocked / Private review / Public / Unknown)

Wolfram embed state (Live · Owner-only · Pending · Diagnostic) — 2 live so far (kora + sambuca)

CAD readiness (Inline glb · External glb · Multi-file glTF · None) — all none right now since the .glb files aren't on disk

Explorer status (ready / awaiting) — 33 ready, 15 awaiting their first generator run

View library-manifest.json — the companion machine-readable inventory the page renders from.

View generate_library.py — the source generator. Rerun anytime with python3 scripts/generate_library.py from instrument-showcase/ and it'll rescan the workspace, refresh both files. Detects CAD readiness automatically (will pick up .glb files when you re-export kora's), reads Wolfram state from manifest first then falls back to wolfram-cloud-sync/manifest/wolfram_embed_urls.json.

Two follow-ups worth knowing:

One explorer wasn't patched: steel-pan — uses a different/older template (the agent noted CSS variables like --steel, --brass instead of the Heifer Zephyr design tokens). Worth deciding whether to regenerate it with your standard generator or hand-patch a Library link.

Bash mount went out of sync again writing into instrument-showcase/data/ and site/ — same WSL caching bug that hit me yesterday. Worked around it by capturing the generator output to /tmp and Write-tooling to Windows. Going forward, anyone running generate_library.py from a regular Windows command prompt or PowerShell will not see this issue; it's specific to my sandbox's WSL filesystem state.

---

# LLM Wiki

A pattern for building personal knowledge bases using LLMs.

This is an idea file, it is designed to be copy pasted to your own LLM Agent (e.g. OpenAI Codex, Claude Code, OpenCode / Pi, or etc.). Its goal is to communicate the high level idea, but your agent will build out the specifics in collaboration with you.

## The core idea

Most people's experience with LLMs and documents looks like RAG: you upload a collection of files, the LLM retrieves relevant chunks at query time, and generates an answer. This works, but the LLM is rediscovering knowledge from scratch on every question. There's no accumulation. Ask a subtle question that requires synthesizing five documents, and the LLM has to find and piece together the relevant fragments every time. Nothing is built up. NotebookLM, ChatGPT file uploads, and most RAG systems work this way.

The idea here is different. Instead of just retrieving from raw documents at query time, the LLM **incrementally builds and maintains a persistent wiki** — a structured, interlinked collection of markdown files that sits between you and the raw sources. When you add a new source, the LLM doesn't just index it for later retrieval. It reads it, extracts the key information, and integrates it into the existing wiki — updating entity pages, revising topic summaries, noting where new data contradicts old claims, strengthening or challenging the evolving synthesis. The knowledge is compiled once and then *kept current*, not re-derived on every query.

This is the key difference: **the wiki is a persistent, compounding artifact.** The cross-references are already there. The contradictions have already been flagged. The synthesis already reflects everything you've read. The wiki keeps getting richer with every source you add and every question you ask.

You never (or rarely) write the wiki yourself — the LLM writes and maintains all of it. You're in charge of sourcing, exploration, and asking the right questions. The LLM does all the grunt work — the summarizing, cross-referencing, filing, and bookkeeping that makes a knowledge base actually useful over time. In practice, I have the LLM agent open on one side and Obsidian open on the other. The LLM makes edits based on our conversation, and I browse the results in real time — following links, checking the graph view, reading the updated pages. Obsidian is the IDE; the LLM is the programmer; the wiki is the codebase.

This can apply to a lot of different contexts. A few examples:

- **Personal**: tracking your own goals, health, psychology, self-improvement — filing journal entries, articles, podcast notes, and building up a structured picture of yourself over time.

- **Research**: going deep on a topic over weeks or months — reading papers, articles, reports, and incrementally building a comprehensive wiki with an evolving thesis.

- **Reading a book**: filing each chapter as you go, building out pages for characters, themes, plot threads, and how they connect. By the end you have a rich companion wiki. Think of fan wikis like [Tolkien Gateway](https://tolkiengateway.net/wiki/Main_Page) — thousands of interlinked pages covering characters, places, events, languages, built by a community of volunteers over years. You could build something like that personally as you read, with the LLM doing all the cross-referencing and maintenance.

- **Business/team**: an internal wiki maintained by LLMs, fed by Slack threads, meeting transcripts, project documents, customer calls. Possibly with humans in the loop reviewing updates. The wiki stays current because the LLM does the maintenance that no one on the team wants to do.

- **Competitive analysis, due diligence, trip planning, course notes, hobby deep-dives** — anything where you're accumulating knowledge over time and want it organized rather than scattered.

## Architecture

There are three layers:

**Raw sources** — your curated collection of source documents. Articles, papers, images, data files. These are immutable — the LLM reads from them but never modifies them. This is your source of truth.

**The wiki** — a directory of LLM-generated markdown files. Summaries, entity pages, concept pages, comparisons, an overview, a synthesis. The LLM owns this layer entirely. It creates pages, updates them when new sources arrive, maintains cross-references, and keeps everything consistent. You read it; the LLM writes it.

**The schema** — a document (e.g. CLAUDE.md for Claude Code or AGENTS.md for Codex) that tells the LLM how the wiki is structured, what the conventions are, and what workflows to follow when ingesting sources, answering questions, or maintaining the wiki. This is the key configuration file — it's what makes the LLM a disciplined wiki maintainer rather than a generic chatbot. You and the LLM co-evolve this over time as you figure out what works for your domain.

## Operations

**Ingest.** You drop a new source into the raw collection and tell the LLM to process it. An example flow: the LLM reads the source, discusses key takeaways with you, writes a summary page in the wiki, updates the index, updates relevant entity and concept pages across the wiki, and appends an entry to the log. A single source might touch 10-15 wiki pages. Personally I prefer to ingest sources one at a time and stay involved — I read the summaries, check the updates, and guide the LLM on what to emphasize. But you could also batch-ingest many sources at once with less supervision. It's up to you to develop the workflow that fits your style and document it in the schema for future sessions.

**Query.** You ask questions against the wiki. The LLM searches for relevant pages, reads them, and synthesizes an answer with citations. Answers can take different forms depending on the question — a markdown page, a comparison table, a slide deck (Marp), a chart (matplotlib), a canvas. The important insight: **good answers can be filed back into the wiki as new pages.** A comparison you asked for, an analysis, a connection you discovered — these are valuable and shouldn't disappear into chat history. This way your explorations compound in the knowledge base just like ingested sources do.

**Lint.** Periodically, ask the LLM to health-check the wiki. Look for: contradictions between pages, stale claims that newer sources have superseded, orphan pages with no inbound links, important concepts mentioned but lacking their own page, missing cross-references, data gaps that could be filled with a web search. The LLM is good at suggesting new questions to investigate and new sources to look for. This keeps the wiki healthy as it grows.

## Indexing and logging

Two special files help the LLM (and you) navigate the wiki as it grows. They serve different purposes:

**index.md** is content-oriented. It's a catalog of everything in the wiki — each page listed with a link, a one-line summary, and optionally metadata like date or source count. Organized by category (entities, concepts, sources, etc.). The LLM updates it on every ingest. When answering a query, the LLM reads the index first to find relevant pages, then drills into them. This works surprisingly well at moderate scale (~100 sources, ~hundreds of pages) and avoids the need for embedding-based RAG infrastructure.

**log.md** is chronological. It's an append-only record of what happened and when — ingests, queries, lint passes. A useful tip: if each entry starts with a consistent prefix (e.g. `## [2026-04-02] ingest | Article Title`), the log becomes parseable with simple unix tools — `grep "^## \[" log.md | tail -5` gives you the last 5 entries. The log gives you a timeline of the wiki's evolution and helps the LLM understand what's been done recently.

## Optional: CLI tools

At some point you may want to build small tools that help the LLM operate on the wiki more efficiently. A search engine over the wiki pages is the most obvious one — at small scale the index file is enough, but as the wiki grows you want proper search. [qmd](https://github.com/tobi/qmd) is a good option: it's a local search engine for markdown files with hybrid BM25/vector search and LLM re-ranking, all on-device. It has both a CLI (so the LLM can shell out to it) and an MCP server (so the LLM can use it as a native tool). You could also build something simpler yourself — the LLM can help you vibe-code a naive search script as the need arises.

## Tips and tricks

- **Obsidian Web Clipper** is a browser extension that converts web articles to markdown. Very useful for quickly getting sources into your raw collection.

- **Download images locally.** In Obsidian Settings → Files and links, set "Attachment folder path" to a fixed directory (e.g. `raw/assets/`). Then in Settings → Hotkeys, search for "Download" to find "Download attachments for current file" and bind it to a hotkey (e.g. Ctrl+Shift+D). After clipping an article, hit the hotkey and all images get downloaded to local disk. This is optional but useful — it lets the LLM view and reference images directly instead of relying on URLs that may break. Note that LLMs can't natively read markdown with inline images in one pass — the workaround is to have the LLM read the text first, then view some or all of the referenced images separately to gain additional context. It's a bit clunky but works well enough.

- **Obsidian's graph view** is the best way to see the shape of your wiki — what's connected to what, which pages are hubs, which are orphans.

- **Marp** is a markdown-based slide deck format. Obsidian has a plugin for it. Useful for generating presentations directly from wiki content.

- **Dataview** is an Obsidian plugin that runs queries over page frontmatter. If your LLM adds YAML frontmatter to wiki pages (tags, dates, source counts), Dataview can generate dynamic tables and lists.

- The wiki is just a git repo of markdown files. You get version history, branching, and collaboration for free.

## Why this works

The tedious part of maintaining a knowledge base is not the reading or the thinking — it's the bookkeeping. Updating cross-references, keeping summaries current, noting when new data contradicts old claims, maintaining consistency across dozens of pages. Humans abandon wikis because the maintenance burden grows faster than the value. LLMs don't get bored, don't forget to update a cross-reference, and can touch 15 files in one pass. The wiki stays maintained because the cost of maintenance is near zero.

The human's job is to curate sources, direct the analysis, ask good questions, and think about what it all means. The LLM's job is everything else.

The idea is related in spirit to Vannevar Bush's Memex (1945) — a personal, curated knowledge store with associative trails between documents. Bush's vision was closer to this than to what the web became: private, actively curated, with the connections between documents as valuable as the documents themselves. The part he couldn't solve was who does the maintenance. The LLM handles that.

## Note

This document is intentionally abstract. It describes the idea, not a specific implementation. The exact directory structure, the schema conventions, the page formats, the tooling — all of that will depend on your domain, your preferences, and your LLM of choice. Everything mentioned above is optional and modular — pick what's useful, ignore what isn't. For example: your sources might be text-only, so you don't need image handling at all. Your wiki might be small enough that the index file is all you need, no search engine required. You might not care about slide decks and just want markdown pages. You might want a completely different set of output formats. The right way to use this is to share it with your LLM agent and work together to instantiate a version that fits your needs. The document's only job is to communicate the pattern. Your LLM can figure out the rest.

---
Yes. This is a very good fit. I’d frame it like this:

Your current `library.html` is the **project control surface**: what exists, what is ready, what deliverables each instrument has.

The LLM Wiki should be the **knowledge layer**: what we have learned, why decisions were made, what patterns repeat across instruments, where source knowledge lives, what still needs investigation.

A clean starting architecture:

```text
instrument-showcase/
  site/
    library.html
    wiki.html
  data/
    library-manifest.json
    wiki-manifest.json
  wiki/
    AGENTS.md
    index.md
    log.md
    instruments/
      kora.md
      cajon.md
      shakuhachi.md
    acoustic-classes/
      duct-flute.md
      tuned-shell.md
      harp-lute.md
    fabrication/
      slip-casting.md
      stave-construction.md
      cnc-routing.md
    materials/
      bamboo.md
      ceramic.md
      red-oak.md
    synthesis/
      public-release-blockers.md
      cad-readiness-roadmap.md
      wolfram-model-patterns.md
  raw-sources/
    inbox/
    second-brain/
    obsidian-clips/
    web-clips/
```

The important distinction: `explorer.html` pages are polished per-instrument artifacts. Wiki pages are living, cross-linked engineering memory.

For the library integration, I’d add a fifth status axis beside Wolfram/CAD/Explorer:

```json
{
  "slug": "kora",
  "wiki_status": "active",
  "wiki_path": "../wiki/instruments/kora.html",
  "wiki_source_count": 14,
  "wiki_last_updated": "2026-05-18",
  "wiki_open_questions": 3,
  "wiki_crosslinks": [
    "harp-lute",
    "bridge-geometry",
    "skin-soundboard"
  ]
}
```

Then each library card gets a compact pill:

```text
Wiki · Active · 14 sources · 3 questions
```

And maybe filters:

```text
Wiki: all / missing / stub / active / review-needed
```

The first wiki should not try to ingest everything. I’d start with a **pilot set of 5 instruments** that exercise different knowledge patterns:

1. `kora` — live Wolfram, string/harp-lute system.
2. `sambuca` — live Wolfram, historical/classical instrument.
3. `cajon` — public, practical build workflow.
4. `ceramic-tongue-drum` or `ceramic-hang` — fabrication/material/process-heavy.
5. `shakuhachi` or `flutes` — acoustic-model-heavy wind instrument.

The best first wiki pages are not “summaries of repos.” They should be pages that make the 50+ project portfolio smarter:

```text
wiki/synthesis/acoustic-model-patterns.md
wiki/synthesis/public-release-blockers.md
wiki/synthesis/cad-readiness-roadmap.md
wiki/acoustic-classes/duct-flute.md
wiki/acoustic-classes/tuned-shell.md
wiki/fabrication/slip-casting.md
wiki/instruments/kora.md
```

Your Obsidian/second_brain material can become a source stream, not the wiki itself. I’d keep it as raw source input and let the LLM compile from it into the Heifer Zephyr wiki. That preserves your real notes while producing a cleaner, queryable, brand-aware engineering memory.

For WRFCoin, I’d make a separate wiki:

```text
wrfcoin-whitepaper/
  wiki/
    index.md
    log.md
    protocol/
    economics/
    implementation/
    open-questions/
```

Then link both from a higher-level `knowledge-index.html` or `portfolio-wiki.html`:

```text
Heifer Zephyr Instrument Wiki
WRFCoin Wiki
Second Brain Source Index
```

My suggested first move: scaffold the instrument wiki with `index.md`, `log.md`, `AGENTS.md`, `wiki-manifest.json`, and add lightweight support in `generate_library.py` so `library.html` can show whether each instrument has a wiki page. That gives you the bridge without boiling the whole ocean.
---
yes, please help me with the first move: scaffold the instrument wiki with index.md, log.md, AGENTS.md, wiki-manifest.json, and add lightweight support in generate_library.py so library.html can show whether each instrument has a wiki page. That gives you the bridge without boiling the whole ocean. I think that the wrfcoin wiki should instead live with that development environment on the linux side probbably here "\\wsl$\Ubuntu\home\tony\wrfcoin\docs\wrfcoin-wiki"  lets start with the scaffolding all around and then we can focus in on one at a time so we don't boil the whole ocean
---

Done. The first bridge is in place.

I scaffolded the instrument wiki here:

- [wiki/index.md](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/wiki/index.md)
- [wiki/log.md](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/wiki/log.md)
- [wiki/AGENTS.md](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/wiki/AGENTS.md)
- [data/wiki-manifest.json](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/data/wiki-manifest.json)

I updated [generate_library.py](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/scripts/generate_library.py) so it now:

- scans `wiki/instruments/<slug>.md`
- writes `data/wiki-manifest.json`
- adds wiki fields into `data/library-manifest.json`
- renders a `Wiki` pill, `With wiki` stat, `Wiki Index` nav link, and Wiki filter in [library.html](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/site/library.html)

I also made the generator tolerate a couple older manifest shapes it hit while scanning: Wolfram entries as strings, and status fields as objects.

WRFCoin got its separate Linux-side scaffold here:

- [wrfcoin-wiki/index.md](/home/tony/wrfcoin/docs/wrfcoin-wiki/index.md)
- [wrfcoin-wiki/log.md](/home/tony/wrfcoin/docs/wrfcoin-wiki/log.md)
- [wrfcoin-wiki/AGENTS.md](/home/tony/wrfcoin/docs/wrfcoin-wiki/AGENTS.md)

Verification: `python3 scripts/generate_library.py` now runs cleanly and produced 50 instrument entries, 46 explorers, 2 live Wolfram embeds, and 0 wiki pages so far. That last number is right: the bridge is ready, but we have not ingested the first instrument yet.

One note: `instrument-showcase` currently shows these as untracked files, matching the earlier agent’s untracked library work rather than a committed baseline. Best next move is to create the first real page at `wiki/instruments/kora.md` or `wiki/instruments/cajon.md` and rerun the generator so the library shows the first `Wiki · Stub` or `Wiki · Active` card.

---
Can we try ingesting the first instruments? yes lets create the first real pages for kora and cajon. Also, I believe each musical instrument design packet lives in a repo folder which includes a rich mix of file formats including .md, .txt. word. xlsx, .pdf, .ppt, and more. Will some or all of those files be able to link directly into the musical instrument wiki from their folder within the "C:\Users\Tony\Documents\GitHub" or do md file distillations have to migrate over to the instrument-showcase/wiki folder? How do I populate this index? What are the next steps?
---
Done. We now have the first two real instrument wiki pages:

- [kora.md](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/wiki/instruments/kora.md)
- [cajon.md](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/wiki/instruments/cajon.md)

I also updated:

- [index.md](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/wiki/index.md)
- [log.md](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/wiki/log.md)
- [wiki-manifest.json](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/data/wiki-manifest.json)
- [library.html](/mnt/c/Users/Tony/Documents/GitHub/instrument-showcase/site/library.html)

`library.html` now shows `With wiki: 2`, and the `kora`/`cajon` cards have active wiki pills:

- `Kora`: `Wiki · Active · 10 src · 7 q`
- `Cajon`: `Wiki · Active · 11 src · 6 q`

On the linking question: no, the source files do not need to migrate into `instrument-showcase/wiki`. The better pattern is:

- Keep raw files in each instrument repo as source of truth.
- Link directly from the wiki to source files like `../../../kora/design.md` or `../../../cajon/Cajon-Family-Design.xlsx`.
- Put distilled, cross-linked knowledge in the wiki page.
- Create `wiki/sources/*.md` summaries only when a source is important enough to deserve a stable LLM-readable distillation.

Markdown, CSV, TXT, JSON, HTML, and SVG are easy to read/link directly. XLSX, DOCX, PDF, and PPTX can still be linked directly, and we can extract/distill them when needed. For Obsidian/local use, those links are useful. If you later publish the site publicly, sibling-repo local links will need either GitHub URLs or copied/exported artifacts.

How to populate the index going forward:

1. Pick an instrument repo.
2. Read its `README`, `capstone-manifest.json`, `design.md`, `validation`, risks, and key build files.
3. Create `wiki/instruments/<slug>.md` with frontmatter.
4. Add it to `wiki/index.md`.
5. Append the ingest to `wiki/log.md`.
6. Run `python3 scripts/generate_library.py`.
7. The central library updates the wiki pill automatically.

Next best step: create reusable cross-instrument pages for `[[acoustic-classes/harp-lute]]`, `[[acoustic-classes/box-drum]]`, and `[[synthesis/public-release-blockers]]`, because `kora` and `cajon` already point at them.


